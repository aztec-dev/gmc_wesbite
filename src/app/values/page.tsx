import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Values",
    description: "The values of Glorious Medical Center",
}

export default function Page() {
    return (
        <h1>Values page</h1>
    )
}